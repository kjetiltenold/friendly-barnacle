This folder contains the submission template that `scripts/build_submission.py` copies into `dist/submission/`.

Do not zip this folder directly unless you have also copied a trained weight file and generated `model_config.json` into it.
